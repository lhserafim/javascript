// While é indicado para quando eu não sei por quanto tempo eu terei a repetição
// O while pode nunca acontecer, dependendo se a condicao for verdadeira logo de cara

function enquanto() {
    let contador = 0;
    while (contador < 10) {
        console.log(`Contador: ${contador}`);
        contador ++;
    }
}

enquanto();